<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootFrame"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_primary">

    <!-- Contenu principal -->
    <LinearLayout
        android:id="@+id/homeContent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Titre -->
        <TextView
            android:id="@+id/homeTitle"
            android:text="Sudoku"
            android:textSize="32sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:layout_marginBottom="32dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <!-- Boutons de sélection de difficulté -->
        <LinearLayout
            android:id="@+id/btnEasy"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_palette_number"
            android:gravity="center"
            android:padding="8dp"
            android:layout_marginBottom="16dp">

            <TextView
                android:id="@+id/textEasy"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Facile"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="@color/text_palette" />

            <TextView
                android:id="@+id/recordEasy"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Record: --"
                android:textSize="14sp"
                android:textColor="@color/text_accent"
                android:layout_marginTop="4dp"/>
        </LinearLayout>

        <LinearLayout
            android:id="@+id/btnNormal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_palette_number"
            android:gravity="center"
            android:padding="8dp"
            android:layout_marginBottom="16dp">

            <TextView
                android:id="@+id/textNormal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Normal"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="@color/text_palette" />

            <TextView
                android:id="@+id/recordNormal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Record: --"
                android:textSize="14sp"
                android:textColor="@color/text_accent"
                android:layout_marginTop="4dp"/>
        </LinearLayout>

        <LinearLayout
            android:id="@+id/btnHard"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/bg_palette_number"
            android:gravity="center"
            android:padding="8dp"
            android:layout_marginBottom="16dp">

            <TextView
                android:id="@+id/textHard"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Difficile"
                android:textSize="20sp"
                android:textStyle="bold"
                android:textColor="@color/text_palette" />

            <TextView
                android:id="@+id/recordHard"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Record: --"
                android:textSize="14sp"
                android:textColor="@color/text_accent"
                android:layout_marginTop="4dp"/>
        </LinearLayout>

    </LinearLayout>

</FrameLayout>
